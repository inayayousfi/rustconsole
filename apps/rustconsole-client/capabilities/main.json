{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows the main viewer window to receive stream lifecycle events.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen"]
}
